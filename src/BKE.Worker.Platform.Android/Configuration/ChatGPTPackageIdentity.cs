namespace BKE.Worker.Platform.Android.Configuration;

public static class ChatGPTPackageIdentity
{
    // Requires emulator verification. Kept in one place until observed on the target device.
    public const string CandidatePackageName = "com.openai.chatgpt";
}
